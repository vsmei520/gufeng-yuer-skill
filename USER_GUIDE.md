# 用户安装指南

1. 在插件市场添加 GitHub 仓库 `https://github.com/vsmei520/gufeng-yuer-skill`。
2. 安装 `gufeng-yuer-skill`。
3. 首次连接时完成 OAuth 授权；若宿主没有自动打开授权页，访问 `https://guoxue.073955.com/activate`，填写管理员发放的授权码、邮箱和称呼。
4. 返回 Codex，重新加载插件或新建对话，让客户端重新发起 OAuth，再发送 `使用抖音 AI 短剧生产工作流。` 并提供选题与构思。

授权状态、一机一码绑定和核心规则均由服务器管理。核心文件不在公开 GitHub 仓库。
